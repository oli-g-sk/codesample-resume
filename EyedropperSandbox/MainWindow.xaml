<Window x:Class="EyedropperSandbox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EyedropperSandbox"
        mc:Ignorable="d"
        Title="Eyedropper Sandbox"
        Height="300"
        Width="420"
        WindowStartupLocation="CenterScreen"
        Background="#FFF4F1EA">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Eyedropper Sandbox"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#FF161A1D"/>

        <Border Grid.Row="2"
                Height="88"
                CornerRadius="6"
                BorderBrush="#33161A1D"
                BorderThickness="1"
                Background="White">
            <Grid Margin="14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border x:Name="ColorSwatch"
                        Width="64"
                        Height="64"
                        CornerRadius="4"
                        BorderBrush="#80161A1D"
                        BorderThickness="1"
                        Background="#FFFFFFFF"/>

                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock x:Name="HexText"
                               Text="#FFFFFFFF"
                               FontFamily="Consolas"
                               FontSize="18"
                               Foreground="#FF161A1D"/>
                    <TextBlock Text="Click the picker, wheel to zoom, Esc to cancel."
                               Margin="0,4,0,0"
                               Foreground="#FF555F66"/>
                </StackPanel>
            </Grid>
        </Border>

        <Button Grid.Row="4"
                Width="148"
                Height="38"
                HorizontalAlignment="Left"
                Click="PickColor_OnClick">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="+" FontSize="18" Margin="0,-1,8,0"/>
                <TextBlock Text="Pick Color"/>
            </StackPanel>
        </Button>
    </Grid>
</Window>
